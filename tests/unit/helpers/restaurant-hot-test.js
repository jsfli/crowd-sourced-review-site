
import { restaurantHot } from 'crowd-sourced-review-site/helpers/restaurant-hot';
import { module, test } from 'qunit';

module('Unit | Helper | restaurant hot');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = restaurantHot([42]);
  assert.ok(result);
});

